#pragma once

enum class Result{
    Ok = 0,
    Fail = -1,
    InvalidArg = -2,
    InvalidState = -3,
    Timeout = -4,
};

enum{
    Ok = 0,
    Fail = -1,
    InvalidArg = -2,
    InvalidState = -3,
    Timeout = -4,
};
